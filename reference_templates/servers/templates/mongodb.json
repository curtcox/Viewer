{
  "id": "mongodb",
  "name": "MongoDB",
  "description": "Execute MongoDB database operations with connection management.",
  "definition_cid": "reference_templates/servers/definitions/mongodb.py",
  "operations": [
    "find",
    "find_one",
    "insert_one",
    "insert_many",
    "update_one",
    "update_many",
    "delete_one",
    "delete_many",
    "count"
  ],
  "secrets": [
    "MONGODB_URI"
  ],
  "notes": "Direct MongoDB connection using connection URI. Supports connection_timeout and query_timeout parameters. Dry-run enabled by default."
}
