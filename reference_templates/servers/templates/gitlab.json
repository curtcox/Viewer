{
  "id": "gitlab",
  "name": "GitLab",
  "description": "List projects, issues, and merge requests in GitLab.",
  "definition_cid": "reference_templates/servers/definitions/gitlab.py",
  "operations": [
    "list_projects",
    "get_project",
    "list_issues",
    "get_issue",
    "create_issue",
    "list_merge_requests",
    "get_merge_request"
  ],
  "secrets": [
    "GITLAB_ACCESS_TOKEN"
  ],
  "notes": "Uses a personal access token or OAuth token with api scope. Supports dry-run previews by default. Can be configured for self-hosted GitLab instances via GITLAB_URL (defaults to https://gitlab.com)."
}
