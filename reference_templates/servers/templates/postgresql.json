{
  "id": "postgresql",
  "name": "PostgreSQL",
  "description": "Execute PostgreSQL database queries with connection management.",
  "definition_cid": "reference_templates/servers/definitions/postgresql.py",
  "operations": [
    "query",
    "execute",
    "fetchone",
    "fetchall"
  ],
  "secrets": [
    "POSTGRESQL_HOST",
    "POSTGRESQL_PORT",
    "POSTGRESQL_USER",
    "POSTGRESQL_PASSWORD",
    "POSTGRESQL_DATABASE"
  ],
  "notes": "Direct PostgreSQL connection. Supports connection_timeout and query_timeout parameters. Dry-run enabled by default."
}
