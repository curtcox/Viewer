{
  "id": "sqlalchemy_pool",
  "name": "SQLAlchemy Pool",
  "description": "Execute database queries using SQLAlchemy connection pooling for MySQL, PostgreSQL, or SQLite.",
  "definition_cid": "reference_templates/servers/definitions/sqlalchemy_pool.py",
  "operations": [
    "query",
    "execute"
  ],
  "secrets": [
    "DATABASE_URL"
  ],
  "notes": "Uses SQLAlchemy for connection pooling. Supports MySQL, PostgreSQL, and SQLite. Configure via DATABASE_URL (e.g., postgresql://user:pass@host/db). Dry-run enabled by default."
}
