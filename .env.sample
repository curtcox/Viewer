# SecureApp Environment Configuration
# Copy this file to .env and update the values below

# Database Configuration
# For local PostgreSQL: postgresql://username:password@localhost:5432/database_name
# For Replit: Will be automatically set via DATABASE_URL environment variable
DATABASE_URL=postgresql://secureapp_user:your_password@localhost:5432/secureapp

# Session Secret - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: Use a secure random string in production
SESSION_SECRET=your-super-secret-session-key-generate-a-new-one

# Replit OAuth Configuration
# Get these values from your Replit account settings > Applications
REPL_ID=your-replit-app-id-here

# Optional: Custom OAuth issuer URL (defaults to Replit's official endpoint)
# ISSUER_URL=https://replit.com/oidc

# Optional: Development settings
# FLASK_ENV=development
# FLASK_DEBUG=1

# Optional: Custom port (defaults to 5000)
# PORT=5000

# Optional: Custom host binding (defaults to 0.0.0.0 for Replit compatibility)
# HOST=127.0.0.1