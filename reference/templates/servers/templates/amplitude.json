{
  "id": "amplitude",
  "name": "Amplitude",
  "description": "Send events to Amplitude analytics platform.",
  "definition_cid": "reference/templates/servers/definitions/amplitude.py",
  "operations": [
    "track",
    "identify"
  ],
  "secrets": [
    "AMPLITUDE_API_KEY",
    "AMPLITUDE_SECRET_KEY"
  ],
  "notes": "Uses HTTP API v2 with API key authentication. Supports tracking events and updating user properties. Dry-run enabled by default."
}
