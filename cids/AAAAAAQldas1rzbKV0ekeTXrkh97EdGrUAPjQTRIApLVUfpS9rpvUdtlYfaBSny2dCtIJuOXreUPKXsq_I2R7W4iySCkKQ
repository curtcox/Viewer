<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>HRX Error</title>
    <style>
        body { font-family: system-ui, -apple-system, sans-serif; max-width: 800px; margin: 2rem auto; padding: 0 1rem; background: #1a1a2e; color: #eee; }
        .error { background: #3d1f1f; border-left: 4px solid #f44; padding: 1rem; border-radius: 4px; }
        h1 { color: #f44; }
        a { color: #4ec9b0; }
        code { background: #252536; padding: 0.2rem 0.4rem; border-radius: 3px; }
    </style>
</head>
<body>
    <div class="error">
        <h1>Error</h1>
        <p>{{ message }}</p>
        <p>Archive: <code>{{ archive_cid }}</code></p>
        <p>Path: <code>{{ file_path or '/' }}</code></p>
    </div>
    {% if root_links %}
    <h2>Archive Contents</h2>
    <ul>
        {% for entry in root_links %}
        <li><span>{{ entry.icon }}</span> <a href="{{ entry.link }}">{{ entry.name }}</a></li>
        {% endfor %}
    </ul>
    {% endif %}
    <p><a href="/gateway/hrx/{{ archive_cid }}">Back to archive root</a></p>
</body>
</html>
